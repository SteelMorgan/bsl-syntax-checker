global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'mcp-bsl-server'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['mcp-bsl-server:9090']  # Web UI port
        labels:
          application: 'mcp-bsl-server'

